# CodexMobile 小红书展示视频

这个目录是一个独立的 Remotion 竖屏视频工程，用现有脱敏截图和 README 公开口径生成小红书展示视频。

## 输出规格

- 画幅：1080 x 1920
- 帧率：30fps
- 时长：22 秒
- 成片：`out/codexmobile-xhs-showcase.mp4`

## 命令

```bash
npm install
npm run still
npm run render
```

也可以打开 Remotion Studio 预览：

```bash
npm run studio
```
